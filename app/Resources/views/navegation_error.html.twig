 <nav class="navbar navbar-default navbar-fixed-top" role="navigation" style="margin-bottom: 0">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
    <a class="navbar-brand" href="#">{% trans %}global.name{% endtrans %}</a>
    </div>

    <ul class="nav navbar-top-links navbar-right">
        <!-- /.dropdown -->
        
         
        <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <i class="fa fa-user fa-fw"></i>  <i class="fa fa-caret-down"></i>
            </a>
            <ul class="dropdown-menu dropdown-user">

                {% if app.user and is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li>
                        <a href="#"><i class="fa fa-user fa-fw"></i> {{ app.user.username }}</a>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">
                            <i class="fa fa-sign-out fa-fw"></i> {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('fos_user_security_login') }}">
                            <i class="fa fa-user fa-fw"></i> {{ 'layout.login'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% endif %}
            </ul>
            <!-- /.dropdown-user -->
        </li>
        <!-- /.dropdown -->
    </ul>
    <!-- /.navbar-top-links -->
    
    {# menús privados ocultos a usuarios no logados #}
    {% if app.user and is_granted("IS_AUTHENTICATED_REMEMBERED") %}
       
    <div class="navbar-default navbar-static-side" role="navigation">
        <div class="sidebar-collapse">
            <ul class="nav" id="side-menu">
                
                <li>
                    <a href="{{ path('jmbermudo_sgr3_homepage') }}"><i class="fa fa-dashboard fa-fw"></i> {% trans %}global.homepage{% endtrans %}</a>
                </li>
                
                {% if app.user and is_granted("ROLE_ADMIN") %}
                {# Incluir aquí todos los accesos a menús de ámbito administrativo #}
                <li>
                    <a href="{{ path('recurso_index') }}"><i class="fa fa-building-o fa-fw"></i> {% trans %}admin.recursos{% endtrans %}</a>
                </li>
                
                {% endif %}
                
            </ul>
            <!-- /#side-menu -->
        </div>
        <!-- /.sidebar-collapse -->
    </div>
    
    {% endif %}
    {# Fin de menús privados ocultos a no identificados #}       
    
</nav>